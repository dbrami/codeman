# Dev-only de-identification denylist (real codenames) — never published
dev/private-denylist.txt
# Build artifacts and machine-specific implementation notes
docs/plans/
# Per-repo destrier config created by commands at runtime
.destrier/
# Dependencies / vendored external tools (installed by /destrier-setup)
node_modules/
# OS / language noise
*.log
.DS_Store
__pycache__/
*.pyc
